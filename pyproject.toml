[project]
name = "silo"
version = "0.1.0"
description = "Local-first version manager"
requires-python = ">=3.11"
dependencies = [
    "click>=8.4.1",
    "questionary>=2.1.1",
]

[project.scripts]
silo = "silo.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
